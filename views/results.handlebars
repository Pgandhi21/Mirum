<div class="results">
  {{#each data}}
  <article class="card">
    <div class="card-header flex-row justify-space-between">
      <h2>
          {{title}}
      </h2>
    </div>
    <div class="card-body">
      <p>{{body}}</p>
      <a href="{{link}}">{{link}}</a>
      <p>{{date}}</p>
    </div>
  </article>
  {{/each}}
</div>